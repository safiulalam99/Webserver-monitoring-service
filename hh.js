function calcSquare(){

	var input = 2
	// var btn = document.getElementById('result');
	var square = btn.value = (input * input);
	console.log('The square of ' + input + ' is ' + square );

	
	
}
calcSquare()
